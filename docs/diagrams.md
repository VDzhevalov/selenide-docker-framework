# 📊 Схеми та діаграми для проєкту

Цей документ містить візуалізації архітектури, CI/CD потоку та локального запуску автотестів.

---

## 🧪 Локальний запуск через CLI

```
 +-----------------------+
 | ./cli.sh tests create|  <-- збірка образу тестів
 +-----------+-----------+
             |
             v
 +----------------------+
 | ./cli.sh tests run   |  <-- запускає тести
 | ./build/reports/...  |
 +-----------+----------+
             |
             v
 +----------------------+
 | ./cli.sh web run     |  <-- запускає вебсервер
 | /build/reports/...   |
 +-----------+----------+
             |
             v
    http://localhost:8080
```

---

## 🔁 CI/CD потік (GitHub Actions)

```
  +------------------+
  |  Push до GitHub  |
  +--------+---------+
           |
           v
+--------------------------+
| GitHub Actions Workflow |
+-----------+--------------+
            |
            v
   +-------------------+        +-----------------+
   | Збірка Dockerfile | -----> |  Запуск тестів  |
   +-------------------+        +--------+--------+
                                       |
                                       v
                         +---------------------------+
                         |  Генерація Allure-звіту   |
                         +-------------+-------------+
                                       |
                                       v
                      +-------------------------------+
                      | Публікація на GitHub Pages    |
                      +-------------------------------+
```

---

## 📦 Зв’язок контейнерів і репорту

```
 [selenide-tests-image]
            |
            v
  Генерує репорт у контейнері:
  /app/build/reports/allure-report
            ||
            || mounted as volume
            vv
  [allure-data] або ./build/reports/

            ||
            vv
 [selenide-web-image]
  Читає звіт з:
  /app/build/allure-report
```
